// Header.js

import React from 'react';
import './Header.css'; // Assuming you have a corresponding CSS file

const Header = ({ title }) => (
  <header>
    <h1>{title}</h1>
  </header>
);

export default Header;
